animations
{
	enabled = true

	bezier = ease, 0.37, 0.06, 0.5, 1.0
	bezier = softpop, 0.1, 0.6, 0.2, 1.1
	bezier = hardpop, 0.0, 0.75, 0.3, 1.1
	bezier = linear, 1, 1, 1, 1

	animation = windowsIn, 1, 5, softpop, gnomed
	animation = windowsOut, 1, 5, ease, slide bottom
	animation = windowsMove, 1, 5, hardpop, slide

	animation = layers, 1, 5, ease, gnomed
	animation = fase, 1, 3, ease
	animation = border, 1, 8, ease
	animation = borderangle, 1, 30, linear, loop

	animation = workspaces, 1, 5, softpop, slide
}
